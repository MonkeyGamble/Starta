// const outer = document.querySelector('.outer');
// const middle = document.querySelector('.middle');
// const inner = document.querySelector('.inner');

// // let bgColor = elem => {
// // 	console.log(window.getComputedStyle(elem).backgroundColor);
// // };

// // let bgColor = element => {
// // 	console.log(getComputedStyle(element).getPropertyValue('background-color'));
// // };

// let bgColor = (value) => {
// 	console.log(getComputedStyle(document.documentElement).getPropertyValue(value));
// };

// outer.onclick = () => {
// 	bgColor(outer);
// };

// middle.onclick = e => {
// 	e.stopPropagation();
// 	bgColor(middle);
// };

// inner.onclick = e => {
// 	e.stopPropagation();
// 	bgColor(inner);
// };
